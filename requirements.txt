# avatarpipe -- pinned runtime dependencies
# CPU-safe: all packages install and run on a plain laptop without a GPU.
# GPU-specific packages (torch with CUDA) are handled inside the Kaggle notebook.
typer==0.12.3
pydantic==2.7.4
click==8.1.7
rich==13.7.1
pyyaml==6.0.1
pillow==10.3.0
imagehash==4.3.1
numpy==1.26.4
torch==2.3.1
open-clip-torch==2.24.0
diffusers==0.29.2
transformers==4.42.3
accelerate==0.31.0
pytest==8.2.2
pytest-cov==5.0.0
